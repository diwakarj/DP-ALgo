/*
#include <iostream>

using namespace std;

#define N 20

int min(int a, int b)
{
	return (a < b) ? a : b;
}

int binomialcoeffecient(int n, int k)
{
	int c[N + 1][N + 1];

	for (int i = 0; i <= n; i++)
	{
		for (int j = 0; j <= (min(i, j)); j++)
		{
			if (j == 0 || j == i)
				c[i][j] = 1;
			else
				c[i][j] = c[i - 1][j - 1] + c[i - 1][j];
		}
	}
	return c[n][k];

}

int main()
{

	cout<<"the binomial coefficent is :"<<binomialcoeffecient(4, 2);

	getchar();
	return 0;
}
*/
